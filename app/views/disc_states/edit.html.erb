   <center>
    <%= form_for @edit_disc_state do |f| %>
        <form>

            <%= label_tag 'Etat' %> <br />
            <%= f.text_field :name, class: "form-control", placeholder: 'Nom', autocomplete: 'off' %>

            <%= label_tag 'Titre' %> <br />
            <%= f.text_field :coefficient, class: "form-control", placeholder: 'Coefficient', autocomplete: 'off' %>
 

          <%= submit_tag "Modifier le style", class: "btn btn-primary" %>  <%= link_to "Revenir à l'accueil", root_path%>
        
      </form>
    <% end %>

    <%= button_to 'Destroy', disc_state_path(@edit_disc_state.id), method: :delete, data: { confirm: 'Are you sure?' } %>



    <%= form_tag deals_url(action: 'create'), method: "post" do %>
           <%=select_tag "disc_id", options_from_collection_for_select(owner_disc, :id, :title) %> <br>
           <%= submit_tag "Proposer un troc", class:"btn btn-info" %>

                     <%=select_tag "disc_id", options_from_collection_for_select(owner_disc, :id, :title) %> <br>
           <%= submit_tag "Proposer un troc", class:"btn btn-info" %>
<%end%>
    </center>